echo node_modules/ > .gitignore
echo .env >> .gitignore
echo build/ >> .gitignore

# 3. decirle a git que deje de rastrear node_modules
git rm -r --cached node_modules

# 4. guardar cambios
git add .gitignore
git commit -m "Fix gitignore and remove node_modules (frontend)"
git push